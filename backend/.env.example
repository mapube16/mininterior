# Railway inyecta DATABASE_URL al enlazar Postgres. En local, SQLite basta.
DATABASE_URL=sqlite:///./rupn.db
# Cambiar en producción por un secreto real.
JWT_SECRET=cambiar-en-produccion
JWT_HORAS=12
# Fuerza el fallo del documental para demostrar PENDIENTE_RADICACION (§11).
CONTROLDOC_FALLA=false
